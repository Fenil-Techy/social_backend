<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
  <div class="bg-zinc-900 w-full min-h-screen">
    <div class="flex justify-between items-center p-10">
      <a href="/" class="text-blue-500 text-lg">Back to home</a>
      <a href="/logout" class="text-red-500 text-lg">Logout</a>
    </div>
    <div class="flex items-center border-b border-zinc-600 p-10 gap-15">
      <div class="w-50 h-50 bg-zinc-800 rounded-full">
        <img src="" alt="" class="">
      </div>
      <div class="text-white">
        <h1 class="text-3xl"><%= user.username %></h1>
        <p class="pt-5 text-lg"><%= user.name %></p>
      </div>
    </div>
    <div class="p-10 grid grid-cols-4 gap-5 place-items-center">
      <div class="bg-zinc-900 w-80 p-4 outline outline-zinc-700 rounded-2xl flex  flex-col gap-5 ">
        <div class="w-full h-50 bg-zinc-800 rounded-2xl">
          <!-- <img src="<%= user.image %>" alt="" class="w-full h-full object-cover rounded-2xl"> -->
        </div>
        <h1 class="text-blue-500 font-semibold text-lg">@<%= user.username %></h1>
            <p class="text-white h-20 overflow-y-auto">Lorem ipsum, dolor sit amet consectetur adipisicing elit. Placeat, odio.</p>
        <!-- <p class="text-zinc-600 "><%= user.email %></p> -->
        <div class="flex justify-between">
          <a href="/edit/<%= user._id %>" class="text-green-500">Edit</a>
          <a href="/delete/<%= user._id %>" class="text-red-500">Delete</a>
        </div>
      </div>
    </div>
  </div>
</body>

</html>